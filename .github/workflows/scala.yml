name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: coursier/cache-action@v4
      - name: Set up JDK
        uses: olafurpg/setup-scala@v7
        with:
          java-version: adopt@1.14
      - name: Run compile
        run: csbt headerCheck scalafmtCheckAll +compile +test